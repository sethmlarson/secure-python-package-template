name: "CodeQL"

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  schedule:
    - cron: "0 0 * * 5"

permissions: "read-all"

jobs:
  analyze:
    name: "Analyze"
    runs-on: "ubuntu-latest"
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: ["python"]

    steps:
    - name: "Checkout repository"
      uses: "actions/checkout@a12a3943b4bdde767164f792f33f40b04645d846"

    - name: "Run CodeQL init"
      uses: "github/codeql-action/init@312e093a1892bd801f026f1090904ee8e460b9b6"
      with:
        languages: "${{ matrix.language }}"

    - name: "Run CodeQL autobuild"
      uses: "github/codeql-action/autobuild@312e093a1892bd801f026f1090904ee8e460b9b6"

    - name: "Run CodeQL analyze"
      uses: "github/codeql-action/analyze@312e093a1892bd801f026f1090904ee8e460b9b6"
